[build-system]
requires = ["uv_build>=0.7.8,<0.8.0"]
build-backend = "uv_build"

[project]
name = "django-indieweb"
authors = [
    {name = "Jochen Wersdörfer", email = "jochen-indieweb@wersdoerfer.de"},
]
version = "0.5.3"
description = "includes indieauth, micropub, and webmention endpoints"
requires-python = ">=3.10"
keywords = [
    "indieweb",
    "indieauth",
    "micropub",
    "webmention",
    "django",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python",
    "Framework :: Django",
    "License :: OSI Approved :: BSD License",
    "Topic :: Internet",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Internet :: WWW/HTTP :: Dynamic Content",
    "Development Status :: 3 - Alpha",
    "Environment :: Web Environment",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Framework :: Django :: 5.2",
    "Framework :: Django :: 6.0",
]

dependencies = [
    "Django>=5.2.13,<6.1",
    "mf2py",
    "beautifulsoup4",
    "httpx",
]

[dependency-groups]
dev = [
    "django-environ",
    "django-stubs[compatible-mypy]",
    "furo",
    "mypy",
    "pytest>=9.0.3",
    "pytest-cov >= 3",
    "pytest-django",
    "ruff",
    "requests>=2.33.0",
    "tox",
    "types-requests",
    "sphinxcontrib-mermaid>=1.0.0",
    "prek>=0.3.10",
]

[project.urls]
Documentation = "https://django-indieweb.readthedocs.io/en/latest/"
Source = "https://github.com/ephes/django-indieweb"

[tool.uv.build-backend]
module-name = "indieweb"

[tool.ruff]
line-length = 119
target-version = "py310"
src = ["src"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C90",  # mccabe
    "UP",   # pyupgrade
    "DJ",   # flake8-django
]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "tests.settings"
minversion = "6.0"
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]
addopts = [
    "--reuse-db",
    "--no-migrations",
    "--cov=indieweb",
    "--cov-config=pyproject.toml",
    "--cov-report=term-missing",
]
norecursedirs = [
    ".git",
    "docs",
]

[tool.coverage.run]
source = ["src/indieweb"]
omit = [
    "*/migrations/*",
    "*/tests/*",
    "*/__init__.py",
    "*/settings.py",
    "*/wsgi.py",
    "*/asgi.py",
]

[tool.coverage.report]
# Baseline measured with `uv run pytest --cov=indieweb --cov-report=term-missing`
# on 2026-05-03: 89% total coverage. Keep the gate conservative to avoid
# insignificant local rounding/noise failures.
fail_under = 88
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
strict_optional = true
disallow_untyped_defs = true
check_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true
files = ["src/indieweb"]
plugins = ["mypy_django_plugin.main"]

[[tool.mypy.overrides]]
module = "environ"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "mf2py"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[[tool.mypy.overrides]]
module = "indieweb.handlers_example"
ignore_errors = true

[[tool.mypy.overrides]]
module = "*.migrations.*"
ignore_errors = true

[tool.django-stubs]
django_settings_module = "tests.settings"
